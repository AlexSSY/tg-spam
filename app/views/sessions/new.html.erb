<div class="h-screen flex items-center justify-center">
  <div class="w-full mx-6">
    <div class="space-y-6">
      <!-- Log in text -->
      <div class="space-y-1">
        <p class="text-2xl text-center">Log In</p>
      </div>
      
      <%= render partial: "sessions/form", locals: { user: @user } %>

      <%= component "divider", "or" %>
      <%= component "button", new_registration_path, "Register", variant: "secondary" %>
    </div>
  </div>
</div>